import type { WorkspaceEntitlement, WorkspaceEntitlementSet, WorkspaceFeatureContract, WorkspacePlanContract, WorkspacePlanSnapshot } from './plans';
export const normalizePlan=(value:WorkspacePlanContract):WorkspacePlanContract=>Object.freeze({identifier:value.identifier,productIdentifier:value.productIdentifier,status:value.status,tier:value.tier,expiresAt:value.expiresAt});
export const normalizeFeature=(value:WorkspaceFeatureContract):WorkspaceFeatureContract=>Object.freeze({identifier:value.identifier,status:value.status});
export const normalizeEntitlement=(value:WorkspaceEntitlement):WorkspaceEntitlement=>Object.freeze({identifier:value.identifier,featureIdentifier:value.featureIdentifier,type:value.type,enabled:value.enabled,limit:value.limit?Object.freeze({amount:value.limit.amount,period:value.limit.period}):null,regions:Object.freeze([...value.regions])});
export const normalizeEntitlementSet=(value:WorkspaceEntitlementSet):WorkspaceEntitlementSet=>Object.freeze({items:Object.freeze(value.items.map(normalizeEntitlement))});
export const normalizePlanSnapshot=(value:WorkspacePlanSnapshot):WorkspacePlanSnapshot=>Object.freeze({schemaVersion:1,plan:normalizePlan(value.plan),features:Object.freeze(value.features.map(normalizeFeature)),entitlements:normalizeEntitlementSet(value.entitlements),accessCapabilities:Object.freeze(value.accessCapabilities.map(item=>Object.freeze({featureIdentifier:item.featureIdentifier,available:item.available})))});
