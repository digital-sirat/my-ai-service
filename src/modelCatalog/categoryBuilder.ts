import{MODEL_CATEGORIES,type ModelCategory}from'@/modelRegistry';import{CATEGORY_SECTION_MAPPING}from'@/navigation/v2/categoryNavigation';import type{CatalogSection,ModelCardContract}from'./types';export const buildCategorySections=(m:readonly ModelCardContract[]):readonly CatalogSection[]=>Object.freeze(MODEL_CATEGORIES.map((c:ModelCategory)=>Object.freeze({id:`category:${c}`,kind:'category' as const,titleKey:`catalog.category.${c}`,productSection:CATEGORY_SECTION_MAPPING[c],models:Object.freeze(m.filter(x=>x.category===c))})));
