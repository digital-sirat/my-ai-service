import type{ModelCapability,ModelCategory}from'@/modelRegistry';import type{CatalogGroup,ModelCardContract}from'./types';export const groupCatalog=<K extends string>(m:readonly ModelCardContract[],key:(x:ModelCardContract)=>K):readonly CatalogGroup<K>[]=>Object.freeze([...m.reduce((map,x)=>{const k=key(x),v=map.get(k)??[];v.push(x);map.set(k,v);return map},new Map<K,ModelCardContract[]>())].map(([key,v])=>Object.freeze({key,models:Object.freeze(v)})));export const groupByProvider=(m:readonly ModelCardContract[])=>groupCatalog(m,x=>x.provider);export const groupByCategory=(m:readonly ModelCardContract[])=>groupCatalog<ModelCategory>(m,x=>x.category);export const groupByCapability=(m:readonly ModelCardContract[]):readonly CatalogGroup<ModelCapability>[]=>{const map=new Map<ModelCapability,ModelCardContract[]>;for(const x of m)for(const c of x.capabilities){const v=map.get(c)??[];v.push(x);map.set(c,v)}return Object.freeze([...map].map(([key,v])=>Object.freeze({key,models:Object.freeze(v)})))};
