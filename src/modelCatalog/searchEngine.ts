import type{ModelCardContract}from'./types';const n=(v:string)=>v.trim().toLocaleLowerCase();export const searchCatalog=(m:readonly ModelCardContract[],q:string):readonly ModelCardContract[]=>{const t=n(q).split(/\s+/).filter(Boolean);if(!t.length)return m;return m.filter(x=>{const s=n([x.id,x.legacyId,x.title,x.description??'',x.provider,x.category,x.productSection,...x.capabilities].join(' '));return t.every(y=>s.includes(y))})};
