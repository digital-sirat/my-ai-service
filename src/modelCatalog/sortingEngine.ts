import type{CatalogSort,ModelCardContract}from'./types';const SORT_FIELD={name:'title',provider:'provider',category:'category'}as const satisfies Readonly<Record<'name'|'provider'|'category',keyof ModelCardContract>>;export const sortCatalog=(m:readonly ModelCardContract[],s:CatalogSort={by:'registry'}):readonly ModelCardContract[]=>{if(s.by==='relevance'||s.by==='registry')return Object.freeze([...m]);const d=s.direction==='desc'?-1:1;return Object.freeze([...m].sort((a,b)=>d*(s.by==='recent'?(a.recentRank??Number.MAX_SAFE_INTEGER)-(b.recentRank??Number.MAX_SAFE_INTEGER):String(a[SORT_FIELD[s.by]]).localeCompare(String(b[SORT_FIELD[s.by]])))))};
