import type{ModelRegistry}from'@/modelRegistry';import{NAVIGATION_SCHEMA}from'../navigationSchema';import{canAccessNavigation}from'./accessControl';import{getModelsForSection}from'./categoryNavigation';import{resolveSectionDestination}from'./routeResolver';import type{NavigationBuildContext,NavigationTree,NavigationTreeNode}from'./types';export interface NavigationBuilder{build(c:NavigationBuildContext):NavigationTree}export const createNavigationBuilder=(r:ModelRegistry):NavigationBuilder=>Object.freeze({build:({access})=>{const nodes=Object.freeze(NAVIGATION_SCHEMA.filter(i=>canAccessNavigation(i.access,access)).map(i=>Object.freeze({...i,canonicalPath:i.path,destination:resolveSectionDestination(i.id,access.enabledCapabilities),models:Object.freeze([...getModelsForSection(r,i.id)]),children:Object.freeze([])} satisfies NavigationTreeNode)));return Object.freeze({version:2 as const,nodes,bySection:new Map(nodes.map(n=>[n.id,n]))})}});