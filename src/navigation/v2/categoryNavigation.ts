import type{ModelCategory,ModelRegistry}from'@/modelRegistry';import type{ProductSection}from'../productSections';import type{NavigationModelLink}from'./types';export const CATEGORY_SECTION_MAPPING:Readonly<Record<ModelCategory,ProductSection>>=Object.freeze({chat:'chat',images:'images',video:'video',music:'music',voice:'voice'});export const getModelsForSection=(r:ModelRegistry,s:ProductSection):readonly NavigationModelLink[]=>r.all().filter(m=>CATEGORY_SECTION_MAPPING[m.category]===s).map(m=>Object.freeze({modelId:m.id,legacyModelId:m.legacyId,provider:m.provider,name:m.name,capabilities:m.capabilities}));