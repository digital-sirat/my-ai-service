import{LEGACY_ROUTE_CONTRACT}from'../routeContract';import type{ProductSection}from'../productSections';export interface LegacyNavigationEntry{id:string;routeName:string;path:string;section:ProductSection;capability?:string}/** Headless adapter; Navigator.vue remains unchanged. */export const adaptLegacyNavigation=():readonly LegacyNavigationEntry[]=>Object.freeze(LEGACY_ROUTE_CONTRACT.filter(r=>r.section).map(r=>Object.freeze({id:`legacy:${r.name}`,routeName:r.name,path:r.path,section:r.section as ProductSection,capability:r.capability})));