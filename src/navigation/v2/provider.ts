import{isNavigationV2Enabled}from'../featureFlags';import type{NavigationBuildContext,NavigationTree}from'./types';export interface NavigationProvider{isEnabled():boolean;load(c:NavigationBuildContext):Promise<NavigationTree|null>}export interface NavigationProviderOptions{isEnabled?:()=>boolean;loadBuilder?:()=>Promise<typeof import('./builder')>}/** Builder and registry load only after opt-in. */export const createNavigationProvider=(o:NavigationProviderOptions={}):NavigationProvider=>Object.freeze({isEnabled:o.isEnabled??isNavigationV2Enabled,async load(c){if(!(o.isEnabled??isNavigationV2Enabled)())return null;const b=await(o.loadBuilder??(()=>import('./builder')))(),{modelRegistry}=await import('@/modelRegistry');return b.createNavigationBuilder(modelRegistry).build(c)}});export const navigationV2Provider=createNavigationProvider();