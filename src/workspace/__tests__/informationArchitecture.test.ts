import { describe, expect, it } from 'vitest'; import { WORKSPACE_INFORMATION_ARCHITECTURE, WORKSPACE_MODALITIES } from '../informationArchitecture'; describe('workspace IA',()=>{ it('defines five modalities',()=>{ expect(WORKSPACE_MODALITIES).toEqual(['text','image','video','audio','tools']); expect(WORKSPACE_INFORMATION_ARCHITECTURE).toHaveLength(5); }); it('is immutable',()=>expect(Object.isFrozen(WORKSPACE_INFORMATION_ARCHITECTURE)).toBe(true)); });
