import { describe, expect, it } from 'vitest'; const entries=import.meta.glob('../index.ts',{eager:true,query:'?raw',import:'default'}) as Record<string,string>; describe('public entry',()=>it('does not export presentation graph',()=>{ const source=Object.values(entries)[0]; expect(source).toBeDefined(); expect(source).not.toContain('./foundation'); expect(source).not.toContain('.vue'); }));
